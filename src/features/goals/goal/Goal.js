import React from "react";
import "./Goal.css";

export const Goal = ({ text }) => {
    colors: 
  return (
    <div className="Goal" style={{ backgroundColor: "red" }}>
      <p>{text}</p>
    </div>
  );
};
