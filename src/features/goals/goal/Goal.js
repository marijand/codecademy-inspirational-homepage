import React from "react";
import "./Goal.css";

export const Goal = ({ text }) => {
  colors: return (
    <div className="Goal" style={{ backgroundColor: "#BA7077" }}>
      <p>{text}</p>
    </div>
  );
};
